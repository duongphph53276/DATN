// import express from "express";
// import { createAttribute, createAttributeValue, getAllAttributes, getAttributeValues } from "../controllers/attribute.js";

// const router = express.Router();

// // Attribute routes
// router.post("/attribute",createAttribute);
// router.get("/attribute", getAllAttributes);

// // AttributeValue routes (gắn theo attributeId)
// router.post("/:attributeId/value", createAttributeValue);
// router.get("/:attributeId/value", getAttributeValues);

// export default router;